<%= form_for [refinery, :features_admin, @feature] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @feature,
              :include_object_name => true %>

  <div class='field'>
    <%= f.label :title -%>
    <%= f.text_field :title, :class => 'larger widest' -%>
  </div>

  <div class='field'>
    <%= render '/refinery/admin/wysiwyg',
                :f => f,
                :fields => [:description],
                :object => "features/feature" -%>
  </div>

  <div class='field'>
    <%= f.label :photo -%>
    <%= render '/refinery/admin/image_picker',
               :f => f,
               :field => :photo_id,
               :image => @feature.photo,
               :toggle_image_display => false -%>
  </div>

  <div class='field'>
    <%= f.label :video_url -%>
    <%= f.text_field :video_url -%>
  </div>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.features.admin.features.feature'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @feature.title) -%>
<% end -%>

<% content_for :javascripts do -%>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end -%>
